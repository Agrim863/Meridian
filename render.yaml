services:
  - type: web
    name: meridian-freight-intelligence
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn freight_prediction_service.freight_prediction_service:app --bind 0.0.0.0:$PORT --workers 1 --timeout 180
    healthCheckPath: /api/freight-prediction/health
